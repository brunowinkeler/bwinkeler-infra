# bwinkeler-infra
Private infrastructure repository and canonical source for the bwinkeler.com platform. Manages the shared Caddy reverse proxy, PostgreSQL, external Docker networks, database/role provisioning, encrypted R2 backups, host bootstrap, monitoring, and disaster recovery for independently deployed apps.
